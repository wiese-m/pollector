[project]
name = "pollector"
version = "0.1.0"
description = "Collector app written in Python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "clickhouse-connect[async]>=0.15.0",
]
